{
  "name": "mycloud_p",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build:bundle": "esbuild src/worker.js --bundle --format=esm --platform=neutral --outfile=dist/worker.bundle.js --minify",
    "build:obf": "node scripts/obfuscate.cjs",
    "build": "npm run build:bundle && npm run build:obf",
    "deploy": "npm run build && wrangler publish"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "javascript-obfuscator": "^4.1.0",
    "wrangler": "^3.0.0"
  }
}
