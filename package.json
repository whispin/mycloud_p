{
  "name": "mycloud_p",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build:bundle": "esbuild src/worker.js --bundle --format=esm --platform=neutral --outfile=dist/worker.bundle.js --minify --external:cloudflare:sockets",
    "build:obf": "node scripts/obfuscate.cjs",
    "build": "npm run build:bundle && npm run build:obf",
    "deploy": "npm run build && wrangler publish"
  },
  "devDependencies": {
    "esbuild": "^0.25.10",
    "javascript-obfuscator": "^4.1.1",
    "wrangler": "^4.40.2"
  }
}
